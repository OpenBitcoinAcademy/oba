How does compact block relay (BIP 152) reduce propagation bandwidth?

- It compresses blocks using gzip
- It sends only block headers and skips transactions
- It sends short transaction IDs so the receiver can match transactions from its mempool
