Why are Compact Block Filters (BIP 157/158) more private than Bloom filters (BIP 37)?

- They encrypt the transaction data before sending it
- The server never learns which addresses the client is looking for
- They use Tor to hide the client's IP address
