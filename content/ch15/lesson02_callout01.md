The Miniscript compiler optimizes for witness size, not script size. When multiple Miniscript expressions encode the same policy, the compiler selects the one that produces the smallest expected witness at spend time. Script size is paid once at output creation, but witness size is paid every time the output is spent. Optimizing for witness size reduces the ongoing cost of using the policy.
