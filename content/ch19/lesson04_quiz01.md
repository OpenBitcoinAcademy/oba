How does BitVM verify computation on Bitcoin?

- By executing the computation on every node
- Through fraud proofs: if a claim is false, any verifier can challenge it on-chain
- By storing the computation result in a block
