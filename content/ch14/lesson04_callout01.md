OP_CHECKSIGADD replaced OP_CHECKMULTISIG for a specific reason. OP_CHECKMULTISIG processes signatures in order against keys, making batch verification impossible (the verifier cannot know which key each signature corresponds to until execution). OP_CHECKSIGADD checks one signature-key pair at a time and increments a counter, allowing the verifier to batch all valid pairs together for a single efficient verification.
