PSBTv2 (BIP 370) introduced the Constructor role and moved input/output data into per-map fields instead of embedding them in the global unsigned transaction. Inputs and outputs can be added incrementally, enabling protocols like PayJoin where the receiver adds inputs to a transaction started by the sender. The trade-off is complexity: v2 PSBTs have more fields and require more validation. Most single-user workflows still use v0.
