PSBT is not a transaction format. It is a container format that wraps a transaction along with the metadata needed to complete it. The actual transaction inside a PSBT is a standard Bitcoin transaction, serialized the same way it would be on the network. PSBT adds structure around it so that signers, updaters, and combiners can each contribute their piece without needing to understand the full spending context.
