What distinguishes a soft fork from a hard fork?

- A soft fork requires all nodes to upgrade, while a hard fork does not
- A soft fork tightens rules so old nodes still follow the chain, while a hard fork breaks compatibility with old nodes
- A soft fork is proposed by miners, while a hard fork is proposed by developers
