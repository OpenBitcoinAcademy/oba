BIP32 defines two types of derivation: normal and hardened. Normal derivation allows computing child public keys from the parent public key. Hardened derivation (marked with an apostrophe, like 44') prevents this, adding an extra layer of security. If a normal child private key is compromised along with the parent public key, the parent private key can be computed. Hardened derivation breaks this chain.
