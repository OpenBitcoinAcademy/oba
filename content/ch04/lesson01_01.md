## Digital Fingerprints

A hash function is a digital fingerprint machine. You feed it any data, any size, and it produces a fixed-size output. That output is called a hash, or a digest.

Feed it a single letter, and you get a hash. Feed it an entire book, and you get a hash of the same length.

The hash is deterministic: the same input always produces the same output. Every time, without exception.
