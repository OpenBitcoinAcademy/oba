Bitcoin Script is intentionally limited. It has no loops, no recursion, and no access to external data. Every script terminates in bounded time. This makes it possible for every node to verify every transaction without risk of infinite computation. Despite these constraints, Script can express a wide range of conditions: ownership proofs, time locks, multi-signature requirements, and hash preimage challenges.
